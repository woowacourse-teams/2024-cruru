import styled from '@emotion/styled';

const Wrapper = styled.div`
  width: 100%;
  height: 100%;
  background-color: white;
  display: flex;
  gap: 2.4rem;

  overflow-x: scroll;
  overflow-y: visible;
`;

const S = {
  Wrapper,
};

export default S;
