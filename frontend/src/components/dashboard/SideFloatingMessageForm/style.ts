import styled from '@emotion/styled';

const SideFloatingContainer = styled.div`
  position: fixed;
  right: 1%;
  bottom: 1%;

  width: 45rem;
  z-index: 999;
`;

const S = {
  SideFloatingContainer,
};

export default S;
