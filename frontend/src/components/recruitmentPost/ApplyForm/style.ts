import styled from '@emotion/styled';

const Form = styled.form`
  width: 100%;

  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 3.2rem;
`;

const S = {
  Form,
};

export default S;
