import styled from '@emotion/styled';

const Container = styled.div`
  width: 100%;
  height: 100%;

  display: flex;
  gap: 1.6rem;

  overflow-x: hidden;
`;

const S = {
  Container,
};

export default S;
