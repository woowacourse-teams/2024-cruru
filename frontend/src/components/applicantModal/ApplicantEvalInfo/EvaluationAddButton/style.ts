import styled from '@emotion/styled';

const ButtonContainer = styled.div`
  width: 100%;
  height: 4.4rem;
  margin: 2.4rem;

  ${({ theme }) => theme.typography.heading[500]}
`;

const S = {
  ButtonContainer,
};

export default S;
