import styled from '@emotion/styled';

const AppContainer = styled.div`
  padding: 3.2rem 0;
  height: 100%;
`;

const DashboardContainer = styled.div`
  padding: 0 1.6rem;
  height: 100%;
`;

const S = {
  AppContainer,
  DashboardContainer,
};

export default S;
