import styled from '@emotion/styled';

const Wrapper = styled.div`
  padding: 34px 10px;
  background-color: white;
  display: flex;
  flex-direction: row;
  gap: 16px;
`;

const S = {
  Wrapper,
};

export default S;
